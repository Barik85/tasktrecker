import React,{Component,Fragment} from 'react';
import PropTypes from 'prop-types';

export default class BlockChat extends Component {
  render() {
    return (
      <Fragment>
        <div>chat</div>
      </Fragment>
    );
  }
}
